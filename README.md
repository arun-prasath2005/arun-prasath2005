# Arun Prasath E G

**CTO who codes. Quant who ships. Agent builder.**

20+ years. 7 exits. 50+ projects and 5,500+ contributions in the last 12 months -- across trading systems, AI agents, SaaS platforms, and security products, built and shipped in parallel.

Anyone can generate code today. A teenager with a laptop can spin up a Next.js app with AI. What they can't do is architect systems that survive production -- enforce security at the boundary layer, design feedback loops that catch regressions before they ship, or make the architectural decisions that separate a prototype from an enterprise product. That takes 20 years of building systems where failure has real consequences.

I build companies, not just code. Every exit was built the same way -- a lean team, end-to-end ownership, and the refusal to outsource anything critical. I've been the one writing the trading engine, deploying the infrastructure, acquiring the customers, and debugging production at 3am.

---

### Why Experience Matters More Than Ever

AI has made code generation trivial. That's exactly why the engineering discipline around it matters more, not less. My autonomous development systems aren't just "AI writes code fast" -- they're architected with the same rigor I bring to any enterprise system:

- **Feedback loops at every stage** -- test gates, static analysis, backpressure mechanisms that halt the pipeline when quality degrades. The AI doesn't just generate; it validates, regresses, and self-corrects.
- **Security-first architecture** -- 20 years of building enterprise products means security isn't an afterthought. Input validation, boundary enforcement, principle of least privilege -- baked into the architecture, not bolted on.
- **Human judgment as the multiplier** -- knowing *what* to build, *how* to decompose a problem, *where* the failure modes are. The AI handles the volume; the architect handles the decisions that matter.

The result: I build multiple products in parallel, each with a clear path to revenue, at a velocity that used to require a 15-person team. But the code that ships is enterprise-grade -- because the system that produces it was designed by someone who's spent two decades learning what enterprise-grade actually means.

---

### AI Agents & Autonomous Systems

This is the core of my work right now.

**Agent Orchestration Frameworks** — Desktop applications and CLI tooling that turn LLMs into autonomous software engineers. Planning mode, build mode, real-time monitoring, tree-sitter AST analysis for codebase understanding, and loop engines that run agents end-to-end with automatic commit/test/iterate cycles. Not wrappers around an API -- full orchestration systems with crash recovery, backpressure, and token tracking.

**Self-Improving Audit Pipelines** — GEPA (Genetic-Pareto) text evolution engines that optimize investigation strategies through a three-stage pipeline: execute candidates, reflect on failure traces with LLM analysis, and curate improved versions via Pareto-efficient search. Static analysis integration, graph-based code exploration, and LLM-as-judge evaluation. Systems that get better at finding bugs each time they run -- reflection over gradients, not brute-force numerical optimization.

**Multi-Agent Architectures** — Production systems where a primary agent spawns parallel sub-agents for concurrent research, each returning structured findings that get synthesized into unified analysis. Claude Agent SDK, MCP servers, tool-use pipelines, and real-time WebSocket streaming. Agents that coordinate, not just respond.

**AI Safety Infrastructure** — Production microservices for prompt injection detection, hallucination validation, and stuck-loop detection (repeater, oscillator, wanderer patterns). Pay-per-call with on-chain micropayments. If you're deploying agents in production, you need guardrails that are as autonomous as the agents themselves.

**Small Model Selection & Fine-Tuning** — Anyone can throw a problem at Opus or Gemini Pro. The real skill is knowing when a fine-tuned 3B parameter model outperforms a 400B one -- at 1/100th the cost and 50x the speed. I select the smallest viable model for each task, fine-tune it with domain-specific data, and build evaluation pipelines that prove it works. This is how you deploy AI in production at scale without burning your margins on API calls.

**DSPy & Conversational AI** — Learnable graph architectures with goal-oriented conversation management, engagement detection, and structured data collection through natural dialogue. Not prompt templates -- optimizable modules with MLflow tracking and training data.

---

### Systems Engineering

**Low-Latency Execution** `Rust` — Sub-10ms order routing through proxy-optimized tunnels. O(1) trigger evaluation engines. Lock-free order book reconstruction over WebSockets. Architecture choices measured in money, not milliseconds.

**On-Chain Protocol Engineering** `Solidity` `Rust` — Flash loan architectures, MEV bundle construction, Dutch auction mechanics, and builder economics optimization. Systems that read intent, calculate profitability atomically, and execute or discard within a single block.

**Full-Stack Product Engineering** `TypeScript` — Multi-tenant SaaS platforms, billing automation, infrastructure-as-code deployments, AI-integrated analytics dashboards, and e-commerce engines. End-to-end -- from Ansible playbooks to customer-facing AI chatbots.

---

### Stack

```
Agents & AI        Claude Code · Claude Agent SDK · MCP · DSPy · Pydantic AI · GEPA
                   Multi-Agent Orchestration · Autonomous Dev Loops · AI Safety
Performance        Rust · WebSockets · SOCKS5 · Lock-Free Structures · Tauri
Smart Contracts    Solidity · Flashbots · Flash Loans · MEV · DeFi Protocols
Web & SaaS         TypeScript · Next.js · TanStack · Medusa
Infrastructure     Docker · Coolify · Railway · Hetzner · GCP · Ansible
Data               PostgreSQL · Redis · ClickHouse · BullMQ · DuckDB
```

---

### Philosophy

Code generation is a commodity. Architecture is not. Anyone can prompt an LLM to produce a function. Knowing which function to write, how it fits into a system that serves 10,000 concurrent users without falling over, and where the security boundaries need to be -- that's the 20-year advantage.

I've engineered the process of engineering itself -- autonomous loops with test gates, quality validation, and feedback mechanisms that enforce the same standards I'd enforce in a code review. The AI handles volume; the architecture ensures every line that ships is production-worthy.

50+ projects. 5,500+ contributions. Not speed for speed's sake -- speed with discipline. The repos you see here are the public surface. Production trading systems, client platforms, and security infrastructure live in private repos where they belong.

---

### Get in Touch

**Email:** arunprasath2005@gmail.com  
**Phone:** +91 99526 68344

<sub>Building something ambitious where lean execution meets AI-native engineering? Let's talk.</sub>
